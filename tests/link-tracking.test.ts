import{describe,expect,it}from"vitest";import{approximateLocation,validCampaign}from"@/lib/link-tracking";describe("link tracking validation",()=>{it("accepts safe campaigns and rejects unsafe ones",()=>{expect(validCampaign("event-2026")).toBe("event-2026");expect(validCampaign("bad campaign!")).toBeNull();});it("uses Cloudflare approximate location without IP data",()=>{const request=new Request("https://saintagram.example/qr");Object.defineProperty(request,"cf",{value:{city:"Batangas City",region:"Batangas",country:"PH",latitude:"13.75",longitude:"121.05"}});const result=approximateLocation(request);expect(result.locationLabel).toBe("Batangas City, Batangas, PH");expect(result.latitude).toBe("13.75");expect(result).not.toHaveProperty("ip");});it("labels localhost without inventing a real location",()=>expect(approximateLocation(new Request("http://localhost:3000/qr"))).toMatchObject({city:"Localhost",region:null,country:null,latitude:null,longitude:null,locationSource:"localhost"}));it("falls back safely",()=>expect(approximateLocation(new Headers()).locationLabel).toBe("Location unavailable"));});
