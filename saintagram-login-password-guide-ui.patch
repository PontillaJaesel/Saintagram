diff --git a/app/auth/page.tsx b/app/auth/page.tsx
index 0b3dea6..e5d071e 100644
--- a/app/auth/page.tsx
+++ b/app/auth/page.tsx
@@ -4,7 +4,6 @@ import {
   FormEvent,
   Suspense,
   useEffect,
-  useMemo,
   useRef,
   useState
 } from "react";
@@ -48,16 +47,6 @@ function AuthForm() {
   const usernameRef = useRef<HTMLInputElement>(null);
   const passwordRef = useRef<HTMLInputElement>(null);
 
-  const copy = useMemo(
-    () => ({
-      eyebrow: "Welcome back",
-      title: "Return to your reflection.",
-      description:
-        "Use the one-time credentials below, then choose a permanent password before continuing."
-    }),
-    []
-  );
-
   const navigateAfterLogin = (nextUser: Awaited<ReturnType<typeof auth.login>>) => {
     const destination = resolvePostAuthRoute(nextUser);
     const requestedNext = searchParams.get("next");
@@ -241,18 +230,12 @@ function AuthForm() {
           </div>
           <ThemeToggle />
         </div>
-        <div className="mx-auto flex w-full max-w-md flex-1 flex-col justify-center py-10">
-          <p className="eyebrow">{copy.eyebrow}</p>
-          <h1 className="mt-3 font-serif text-4xl font-bold tracking-tight sm:text-5xl">
-            {copy.title}
+        <div className="mx-auto flex w-full max-w-md flex-1 -translate-y-6 flex-col justify-center py-10 sm:-translate-y-8 lg:-translate-y-4">
+          <h1 className="font-serif text-4xl font-bold tracking-tight sm:text-5xl">
+            Log in
           </h1>
-          {copy.description && (
-            <p className="mt-4 text-base leading-7 text-muted">
-              {copy.description}
-            </p>
-          )}
 
-          <form className="mt-8 space-y-5" onSubmit={submit} noValidate>
+          <form className="mt-6 space-y-5" onSubmit={submit} noValidate>
             <div>
               <label htmlFor="username" className="label">
                 Username
@@ -293,7 +276,7 @@ function AuthForm() {
             <div>
               <div className="flex items-center justify-between">
                 <label htmlFor="password" className="label">
-                  Temporary password
+                  Password
                 </label>
               </div>
               <div className="relative">
@@ -337,7 +320,7 @@ function AuthForm() {
                 </button>
               </div>
               <p id="password-help" className="mt-2 text-xs text-muted">
-                Your one-time temporary password is required to sign in.
+                Enter your password to sign in.
               </p>
             </div>
             {error && (
diff --git a/components/settings/settings-panel.tsx b/components/settings/settings-panel.tsx
index a0cba10..f180b21 100644
--- a/components/settings/settings-panel.tsx
+++ b/components/settings/settings-panel.tsx
@@ -462,6 +462,20 @@ export function SettingsPanel() {
             Set a new permanent password before continuing.
           </div>
         )}
+
+        <div className="mb-5 rounded-[var(--radius-base)] border border-sage-200 bg-sage-50/70 p-4">
+          <p className="text-sm font-bold text-ink">How to change your password</p>
+          <ol className="mt-3 space-y-2 text-sm leading-6 text-muted">
+            <li><span className="font-bold text-ink">1.</span> Enter your current password.</li>
+            <li><span className="font-bold text-ink">2.</span> Enter your new password.</li>
+            <li><span className="font-bold text-ink">3.</span> Enter the same password under Confirm new password.</li>
+            <li><span className="font-bold text-ink">4.</span> Select Change password to save your changes.</li>
+          </ol>
+          <p className="mt-3 border-t border-sage-200 pt-3 text-xs font-semibold leading-5 text-muted">
+            Your new password and confirmation password must match.
+          </p>
+        </div>
+
         <form className="space-y-4" onSubmit={submitPassword} noValidate>
           <div>
             <label htmlFor="settings-current-password" className="label">
