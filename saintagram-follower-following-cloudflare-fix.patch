diff --git a/app/api/social/users/[userId]/connections/route.ts b/app/api/social/users/[userId]/connections/route.ts
index a720302..a87f981 100644
--- a/app/api/social/users/[userId]/connections/route.ts
+++ b/app/api/social/users/[userId]/connections/route.ts
@@ -71,7 +71,10 @@
       );
     }
 
-    const verified = await getFirebaseAdminAuth().verifyIdToken(token, true);
+    // Verify the Firebase ID token locally. Do not pass `true` here: that
+    // performs an additional remote revocation/user lookup, which is unreliable
+    // in the Cloudflare Workers runtime and can reject otherwise valid sessions.
+    const verified = await getFirebaseAdminAuth().verifyIdToken(token);
     const { userId } = await params;
     if (!/^[A-Za-z0-9:_-]{1,128}$/.test(userId)) {
       return NextResponse.json(
diff --git a/app/api/social/sync-required-follows/route.ts b/app/api/social/sync-required-follows/route.ts
index e5c90cf..1c588e0 100644
--- a/app/api/social/sync-required-follows/route.ts
+++ b/app/api/social/sync-required-follows/route.ts
@@ -11,7 +11,10 @@
     if (!token) {
       return NextResponse.json({ error: "Authentication is required." }, { status: 401, headers });
     }
-    const verified = await getFirebaseAdminAuth().verifyIdToken(token, true);
+    // Do not request the extra remote revocation lookup in Cloudflare Workers.
+    // Signature/issuer/audience/expiration verification is sufficient for this
+    // short-lived Firebase ID token and matches the rest of the app's server auth.
+    const verified = await getFirebaseAdminAuth().verifyIdToken(token);
     await syncRequiredFollowsForUser(verified.uid);
     return NextResponse.json({ ok: true }, { headers });
   } catch (error) {
