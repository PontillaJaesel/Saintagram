"use client";import{FIAT_CATEGORIES}from"@/lib/fiat";import type{FiatCategory}from"@/types";
export function FiatCategorySelector({value,onChange}:{value:FiatCategory|undefined;onChange:(value:FiatCategory|undefined)=>void}){return <fieldset className="mt-5"><legend className="text-sm font-bold text-ink">What&apos;s your FiAt today?</legend><p className="mt-1 text-xs leading-5 text-muted">What &apos;yes&apos; did you give to God today? Optional.</p><div className="mt-3 flex flex-wrap gap-2" role="radiogroup" aria-label="FiAt category">{FIAT_CATEGORIES.map(item=><button key={item.value} type="button" role="radio" aria-checked={value===item.value} className={`chip ${value===item.value?'chip-selected':''}`} onClick={()=>onChange(value===item.value?undefined:item.value)}>{item.label}</button>)}</div>{value&&<button type="button" className="mt-2 text-xs font-bold text-muted underline" onClick={()=>onChange(undefined)}>Clear FiAt category</button>}</fieldset>}
